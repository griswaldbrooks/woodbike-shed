# Pinned code-CAD stack (chosen by scout report data/woodbike-shed-cadlib-scout/report.md).
# Python 3.12, install with uv:  uv venv --python 3.12 .venv && uv pip install -r requirements.txt
build123d==0.11.1
cadquery-ocp-novtk==7.9.3.1.1
trimesh==5.0.0
numpy==2.5.1
